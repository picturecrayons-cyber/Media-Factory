# Replaced by ci.yml. Kept empty-trigger-free so Actions does not run a hello-world job on main.
# Name retained only to avoid a confusing leftover "Hello, world!" workflow on this branch.
name: Retired hello workflow
on:
  workflow_dispatch:
jobs:
  skip:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo skipped
